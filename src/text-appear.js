/**
 * Stacked cards paint inner type on first frame — no micro-scale-fade hide,
 * no expand-sheet preloader. Desktop panel copy still uses intro.js.
 */

export function revealCardExpand() {}

export function initTextAppear() {}
